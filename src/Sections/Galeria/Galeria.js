import React from "react";
import "./Galeria.scss";

const Galeria = ({ myRef }) => {
  return (
    <div ref={myRef} className="Galeria-Container">
      Galeria
    </div>
  );
};

export default Galeria;
